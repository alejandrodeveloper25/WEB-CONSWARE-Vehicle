import { Routes } from '@angular/router';
import { VechiclesListComponent } from './pages/vechicles-list/vechicles-list.component';

export const routes: Routes = [{ path: '', component: VechiclesListComponent }];
